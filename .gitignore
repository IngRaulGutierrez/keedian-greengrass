# Certificados y claves del dispositivo IoT (generados automáticamente)
config/private.pem.key
config/device.pem.crt
config/public.pem.key
config/AmazonRootCA1.pem

# Variables de entorno con credenciales
.env
*.env
!.env.example

# Claude Code
.claude/settings.local.json
